spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.202.211:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

# 应用服务 WEB 访问端口
server:
  port: 11000

